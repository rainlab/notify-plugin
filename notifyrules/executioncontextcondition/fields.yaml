# ===================================
#  Field Definitions
# ===================================

fields:

    subcondition:
        label: rainlab.notify::lang.context.subcondition
        span: auto
        type: dropdown

    operator:
        label: rainlab.notify::lang.context.operator
        span: auto
        type: dropdown
        dependsOn: subcondition

    value:
        label: rainlab.notify::lang.context.value
        dependsOn: [subcondition, operator]
        type: dropdown
